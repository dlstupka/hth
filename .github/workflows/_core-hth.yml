name: HTH Core Pipeline
on:
  workflow_call:
    inputs:
      mode: {type: string, required: true}
      process_limit: {type: string, default: "all"}
      run_preprocess: {type: boolean, default: true}
      run_detect: {type: boolean, default: true}
      run_validate: {type: boolean, default: true}
      run_publish: {type: boolean, default: true}

jobs:
  ############################################################
  # Acquisition
  ############################################################

  ############################################################
  # stage_preprocess
  ############################################################

  ############################################################
  # stage_detect
  ############################################################

  ############################################################
  # stage_validate
  ############################################################

  ############################################################
  # stage_publish
  ############################################################
