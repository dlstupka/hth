name: HTH generate report

run-name: >-
  Generate ${{ inputs.report }} report on ${{ inputs.runner }}

on:
  workflow_dispatch:
    inputs:
      report:
        description: Report to regenerate
        required: true
        default: detector-calibration-manifest
        type: choice
        options:
          - detector-calibration-manifest
          - execution-optimizer
      algorithm:
        description: Detector for execution-optimizer report; choose all for the accumulated optimizer summary; ignored by calibration manifest
        required: true
        default: all
        type: choice
        options:
          - all
          - contour
          - contour_quad
          - contour_components
          - contour_projection
          - contour_grabcut
          - grabcut_contour
          - consensus_quad
          - edge_contour
          - cross_edge_contour
          - gradient_vote
          - radial_edge
          - adaptive_radial_edge
          - border_energy
          - components
          - ransac
          - grabcut
          - hough
          - lsd
      results_repository:
        description: Results repository containing persisted intelligence
        required: true
        default: dlstupka/hth-baptisms-san-antonio-1788-1824--1858-1898-results
        type: string
      golden_set:
        description: Golden Set used to select compatible detector calibrations
        required: true
        default: config/golden_set.json
        type: string
      runner:
        description: Execution runner
        required: true
        default: github-hosted
        type: choice
        options:
          - github-hosted
          - self-hosted-hth
          - self-hosted-windows
          - self-hosted-rhel8
          - self-hosted-e7k
          - self-hosted-e9k
      specific_runner:
        description: "Specific self-hosted runner; any keeps the selected runner class"
        required: true
        default: any
        type: choice
        options:
          - any
          - rh8-al320
          - rh8-al97
          - rh8-s32

permissions:
  contents: read

concurrency:
  group: generate-report-${{ github.ref }}-${{ inputs.report }}-${{ inputs.algorithm }}
  cancel-in-progress: false

jobs:
  report:
    uses: ./.github/workflows/_core-hth.yml
    with:
      mode: report
      runner: ${{ inputs.runner }}
      specific_runner: ${{ inputs.specific_runner }}
      results_repository: ${{ inputs.results_repository }}
      report_type: ${{ inputs.report }}
      report_algorithm: ${{ inputs.algorithm }}
      report_golden_set: ${{ inputs.golden_set }}
      publish_results: true
      upload_full_artifact: false
    secrets: inherit
