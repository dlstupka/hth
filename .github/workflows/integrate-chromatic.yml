name: HTH normalize chromatic integration
run-name: Build the validated chromatic-normalized collection for downstream HTR

on:
  workflow_dispatch:
    inputs:
      # BEGIN GENERATED RUNNER TARGET INPUT
      runner_target:
        description: "Execution runner target"
        required: true
        default: github-hosted
        type: choice
        options:
          - github-hosted
          - self-hosted-linux
          - self-hosted-windows
          - hth
          - rhel8
          - e7k
          - e9k
          - 192t
          - 96t
          - 32t
      # END GENERATED RUNNER TARGET INPUT
      artifact_retention_days: {description: "Days to retain compact review evidence", required: true, default: 14, type: number}
      canonical_build_policy:
        description: "Canonical Build Evidence policy"
        required: true
        default: auto
        type: choice
        options: [auto, audit, force-verify, rebuild]
  workflow_call:
    inputs:
      runner_target: {required: true, type: string}
      artifact_retention_days: {required: true, type: number}
      canonical_build_policy: {required: true, type: string}
    secrets:
      HTH_RESULTS_TOKEN: {required: true}

permissions: {contents: read}
concurrency: {group: "integrate-chromatic-${{ github.ref }}", cancel-in-progress: false}
env:
  GIT_CONFIG_COUNT: "1"
  GIT_CONFIG_KEY_0: init.defaultBranch
  GIT_CONFIG_VALUE_0: main
  RESULTS_REPOSITORY: dlstupka/hth-baptisms-san-antonio-1788-1824--1858-1898-results

jobs:
  integrate-chromatic:
    name: Produce the validated chromatic-normalized collection
    # BEGIN GENERATED RUNNER TARGET ROUTING
    runs-on: >-
      ${{ inputs.runner_target == 'self-hosted-linux' && fromJSON('["self-hosted","Linux","X64"]')
        || inputs.runner_target == 'self-hosted-windows' && fromJSON('["self-hosted","Windows","X64"]')
        || inputs.runner_target == 'hth' && fromJSON('["self-hosted","Linux","X64","hth"]')
        || inputs.runner_target == 'rhel8' && fromJSON('["self-hosted","Linux","X64","rhel8"]')
        || inputs.runner_target == 'e7k' && fromJSON('["self-hosted","Linux","X64","e7k"]')
        || inputs.runner_target == 'e9k' && fromJSON('["self-hosted","Linux","X64","e9k"]')
        || inputs.runner_target == '192t' && fromJSON('["self-hosted","Linux","X64","192t"]')
        || inputs.runner_target == '96t' && fromJSON('["self-hosted","Linux","X64","96t"]')
        || inputs.runner_target == '32t' && fromJSON('["self-hosted","Linux","X64","32t"]')
        || 'ubuntu-latest' }}
    # END GENERATED RUNNER TARGET ROUTING
    timeout-minutes: 120
    env:
      # BEGIN GENERATED RUNNER SETUP LABEL
      HTH_SELECTED_RUNNER_LABEL: >-
        ${{ inputs.runner_target == 'self-hosted-linux' && 'linux'
          || inputs.runner_target == 'self-hosted-windows' && 'windows'
          || inputs.runner_target == 'hth' && 'hth'
          || inputs.runner_target == 'rhel8' && 'rhel8'
          || inputs.runner_target == 'e7k' && 'e7k'
          || inputs.runner_target == 'e9k' && 'e9k'
          || inputs.runner_target == '192t' && '192t'
          || inputs.runner_target == '96t' && '96t'
          || inputs.runner_target == '32t' && '32t'
          || 'github-hosted' }}
      # END GENERATED RUNNER SETUP LABEL
    steps:
      - name: Checkout HTH pipeline
        uses: actions/checkout@v6
        with: {path: hth-pipeline, fetch-depth: 1, persist-credentials: false}

      - name: Checkout canonical chromatic and tonal evidence
        uses: actions/checkout@v6
        with:
          repository: ${{ env.RESULTS_REPOSITORY }}
          path: results-repo
          ref: main
          token: ${{ secrets.HTH_RESULTS_TOKEN || github.token }}
          fetch-depth: 1
          sparse-checkout: |
            /normalization/tonal-integration/
            /normalization/chromatic/
            /normalization/chromatic-methods/
            /normalization/chromatic-validation/
            /normalization/chromatic-integration/
          sparse-checkout-cone-mode: false
          persist-credentials: true

      - name: Set up canonical HTH Python runtime
        uses: ./hth-pipeline/.github/actions/setup-hth-python
        with: {pipeline-path: hth-pipeline, runner-label: "${{ env.HTH_SELECTED_RUNNER_LABEL }}"}

      - name: Assemble chromatic effective-input contract
        id: inputs
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$RUNNER_TEMP/chromatic-effective-inputs"
          mkdir -p "$RUNNER_TEMP/chromatic-effective-inputs"
          cp results-repo/normalization/tonal-integration/{release.json,tonal-normalization-manifest.json} "$RUNNER_TEMP/chromatic-effective-inputs/"
          cp results-repo/normalization/chromatic/assessment.json "$RUNNER_TEMP/chromatic-effective-inputs/chromatic-assessment.json"
          cp results-repo/normalization/chromatic-methods/assessment.json "$RUNNER_TEMP/chromatic-effective-inputs/chromatic-method-assessment.json"
          cp results-repo/normalization/chromatic-validation/validation.json "$RUNNER_TEMP/chromatic-effective-inputs/chromatic-validation.json"
          python <<'PY'
          import json, os
          from pathlib import Path
          release = json.loads(Path("results-repo/normalization/tonal-integration/release.json").read_text())
          with Path(os.environ["GITHUB_OUTPUT"]).open("a") as out:
              for source, target in (
                  ("tag", "source_tag"),
                  ("asset", "source_asset"),
                  ("asset_sha256", "source_sha256"),
                  ("tonal_result_identity", "source_identity"),
              ):
                  value = str(release.get(source) or "")
                  if not value:
                      raise SystemExit(f"Tonal release has no {source}")
                  out.write(f"{target}={value}\n")
          PY

      - name: Evaluate chromatic Canonical Build Evidence
        id: cbe_plan
        shell: bash
        env: {PYTHONPATH: hth-pipeline}
        run: |
          set -euo pipefail
          python -m hth.canonical_build_evidence prepare \
            --scope hth-chromatic-integration \
            --policy "${{ inputs.canonical_build_policy }}" \
            --contract-version 1 --mode complete-production \
            --operation consume-immutable-tonal-collection \
            --operation classify-every-page-by-chromatic-metrics \
            --operation apply-only-validated-bounded-chromatic-method \
            --operation preserve-exceptions-and-continue \
            --operation package-immutable-lossless-collection \
            --repository-root hth-pipeline \
            --source-root "$RUNNER_TEMP/chromatic-effective-inputs" \
            --source-repository "$RESULTS_REPOSITORY" \
            --source-release "${{ steps.inputs.outputs.source_tag }}" \
            --source-manifest-sha256 "${{ steps.inputs.outputs.source_sha256 }}" \
            --source-commit "${{ steps.inputs.outputs.source_identity }}" \
            --config hth-pipeline/config/chromatic-assessment.json \
            --config hth-pipeline/config/chromatic-method-assessment.json \
            --config hth-pipeline/config/chromatic-method-validation.json \
            --implementation hth-pipeline/hth/normalize_chromatic.py \
            --runtime-contract hth-pipeline/requirements.txt \
            --runtime-package numpy --runtime-package opencv-python-headless \
            --results-root results-repo \
            --evidence results-repo/normalization/chromatic-integration/canonical-build-evidence.json \
            --plan "$RUNNER_TEMP/chromatic-cbe-plan.json" \
            --pipeline-repository "$GITHUB_REPOSITORY" --pipeline-commit "$GITHUB_SHA" \
            --results-repository "$RESULTS_REPOSITORY" --workflow-run-id "$GITHUB_RUN_ID" \
            --runner-name "$RUNNER_NAME" --runner-environment "${{ runner.environment }}" --runner-os "$RUNNER_OS" --runner-arch "$RUNNER_ARCH" \
            --github-output "$GITHUB_OUTPUT" --github-summary "$GITHUB_STEP_SUMMARY"

      - name: Audit reusable immutable chromatic release
        if: steps.cbe_plan.outputs.decision != 'execute'
        shell: bash
        env:
          GH_TOKEN: "${{ secrets.HTH_RESULTS_TOKEN || github.token }}"
        run: |
          set -euo pipefail
          readarray -t fields < <(python <<'PY'
          import json
          from pathlib import Path
          record = json.loads(Path("results-repo/normalization/chromatic-integration/release.json").read_text())
          for key in ("tag", "asset", "asset_sha256", "chromatic_result_identity"):
              value = str(record.get(key) or "")
              if not value: raise SystemExit(f"Chromatic release record has no {key}")
              print(value)
          PY
          )
          digest="$(gh api "repos/$RESULTS_REPOSITORY/releases/tags/${fields[0]}" --jq ".assets[] | select(.name == \"${fields[1]}\") | .digest")"
          test "$digest" = "sha256:${fields[2]}"
          url="${GITHUB_SERVER_URL}/${RESULTS_REPOSITORY}/releases/tag/${fields[0]}"
          printf '\n## Reusable chromatic result\n\n- Chromatic result: [`%s`](%s)\n- Release activity: `AUDITED`\n- Release: %s\n' "${fields[3]}" "$url" "$url" >> "$GITHUB_STEP_SUMMARY"

      - name: Restore cached immutable tonal collection
        id: tonal_asset
        if: steps.cbe_plan.outputs.decision == 'execute'
        uses: ./hth-pipeline/.github/actions/restore-immutable-release
        with:
          repository: ${{ env.RESULTS_REPOSITORY }}
          tag: ${{ steps.inputs.outputs.source_tag }}
          asset: ${{ steps.inputs.outputs.source_asset }}
          sha256: ${{ steps.inputs.outputs.source_sha256 }}
          token: ${{ secrets.HTH_RESULTS_TOKEN || github.token }}
          destination: tonal-asset
          extract-destination: tonal-input
          collection-marker: tonal-normalization-manifest.json
          required-subdirectory: tonal-normalized

      - name: Integrate validated chromatic correction
        id: integration
        if: steps.cbe_plan.outputs.decision == 'execute'
        shell: bash
        env:
          PYTHONPATH: hth-pipeline
          TONAL_COLLECTION: ${{ steps.tonal_asset.outputs.collection-root }}
          TONAL_MANIFEST: ${{ steps.tonal_asset.outputs.marker-path }}
        run: |
          set -euo pipefail
          rm -rf chromatic-integrated-collection
          python -m hth.normalize_chromatic integrate \
            --collection "$TONAL_COLLECTION" \
            --upstream-manifest "$TONAL_MANIFEST" \
            --assessment results-repo/normalization/chromatic/assessment.json \
            --comparison results-repo/normalization/chromatic-methods/assessment.json \
            --validation results-repo/normalization/chromatic-validation/validation.json \
            --output chromatic-integrated-collection
          cp results-repo/normalization/chromatic/assessment.json chromatic-integrated-collection/chromatic-assessment.json
          cp results-repo/normalization/chromatic-methods/assessment.json chromatic-integrated-collection/chromatic-method-assessment.json
          cp results-repo/normalization/chromatic-validation/validation.json chromatic-integrated-collection/chromatic-validation.json
          python <<'PY'
          import json, os
          from pathlib import Path
          manifest = json.loads(Path("chromatic-integrated-collection/chromatic-normalization-manifest.json").read_text())
          identity = str(manifest.get("chromatic_result_identity") or "")
          if len(identity) != 64: raise SystemExit("Chromatic integration produced no valid identity")
          with Path(os.environ["GITHUB_OUTPUT"]).open("a") as out:
              out.write(f"result_identity={identity}\nrelease_tag=HTH-CHROMATIC-{identity}\n")
          PY

      - name: Summarize chromatic integration decision
        shell: bash
        env: {PYTHONPATH: hth-pipeline}
        run: |
          if [[ "${{ steps.cbe_plan.outputs.decision }}" == execute ]]; then
            input=chromatic-integrated-collection/chromatic-normalization-manifest.json
          else
            input=results-repo/normalization/chromatic-integration/chromatic-normalization-manifest.json
          fi
          python -m hth.chromatic_summary \
            --stage integrate \
            --input "$input" \
            --github-summary "$GITHUB_STEP_SUMMARY"

      - name: Package immutable chromatic collection
        id: package
        if: steps.cbe_plan.outputs.decision == 'execute'
        shell: bash
        env: {PYTHONPATH: hth-pipeline}
        run: |
          set -euo pipefail
          asset="$RUNNER_TEMP/hth-chromatic-${{ steps.integration.outputs.result_identity }}.zip"
          python -m hth.normalize_chromatic package --collection chromatic-integrated-collection --asset "$asset" --tag "${{ steps.integration.outputs.release_tag }}" --output-record chromatic-integrated-collection/release.json
          python <<'PY'
          import json, os
          from pathlib import Path
          record = json.loads(Path("chromatic-integrated-collection/release.json").read_text())
          with Path(os.environ["GITHUB_OUTPUT"]).open("a") as out:
              out.write(f"asset={Path(os.environ['RUNNER_TEMP']) / record['asset']}\nasset_sha256={record['asset_sha256']}\n")
          PY

      - name: Finalize chromatic Canonical Build Evidence
        id: cbe_result
        if: steps.cbe_plan.outputs.decision == 'execute'
        shell: bash
        env: {PYTHONPATH: hth-pipeline}
        run: |
          set -euo pipefail
          python -m hth.canonical_build_evidence finalize --plan "$RUNNER_TEMP/chromatic-cbe-plan.json" --output-root chromatic-integrated-collection --evidence-store results-repo/normalization/chromatic-integration/canonical-build-evidence.json --evidence-output chromatic-integrated-collection/canonical-build-evidence.json --github-output "$GITHUB_OUTPUT" --github-summary "$GITHUB_STEP_SUMMARY"

      - name: Publish or reuse immutable chromatic release
        id: release
        if: steps.cbe_plan.outputs.decision == 'execute'
        shell: bash
        env:
          GH_TOKEN: "${{ secrets.HTH_RESULTS_TOKEN }}"
          RELEASE_TAG: "${{ steps.integration.outputs.release_tag }}"
          RELEASE_ASSET: "${{ steps.package.outputs.asset }}"
          ASSET_SHA256: "${{ steps.package.outputs.asset_sha256 }}"
          RESULT_IDENTITY: "${{ steps.integration.outputs.result_identity }}"
        run: |
          set -euo pipefail
          asset_name="hth-chromatic-${RESULT_IDENTITY}.zip"
          if gh release view "$RELEASE_TAG" --repo "$RESULTS_REPOSITORY" >/dev/null 2>&1; then
            digest="$(gh api "repos/$RESULTS_REPOSITORY/releases/tags/$RELEASE_TAG" --jq ".assets[] | select(.name == \"$asset_name\") | .digest")"
            test "$digest" = "sha256:$ASSET_SHA256" || { echo "Existing chromatic release does not match deterministic rebuild" >&2; exit 1; }
            activity=REUSED
          else
            gh release create "$RELEASE_TAG" "$RELEASE_ASSET" --repo "$RESULTS_REPOSITORY" --target main --title "HTH chromatic normalized collection $RESULT_IDENTITY" --notes "Deterministic selective background-color normalization. Every page continues downstream."
            activity=CREATED
          fi
          echo "release_activity=$activity" >> "$GITHUB_OUTPUT"
          echo "release_url=${GITHUB_SERVER_URL}/${RESULTS_REPOSITORY}/releases/tag/${RELEASE_TAG}" >> "$GITHUB_OUTPUT"

      - name: Publish compact canonical chromatic evidence
        id: publish
        if: steps.cbe_plan.outputs.decision == 'execute'
        shell: bash
        env: {PYTHONPATH: hth-pipeline}
        run: |
          set -euo pipefail
          source hth-pipeline/tools/hardened-persistence.sh
          git -C results-repo config user.name "github-actions[bot]"
          git -C results-repo config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          payload="$RUNNER_TEMP/chromatic-payload"
          rm -rf "$payload"; mkdir -p "$payload"
          cp chromatic-integrated-collection/{chromatic-normalization-manifest.json,chromatic-normalization-manifest.csv,summary.md,chromatic-assessment.json,chromatic-method-assessment.json,chromatic-validation.json,release.json} "$payload/"
          incoming="$RUNNER_TEMP/chromatic-cbe-incoming.json"
          cp chromatic-integrated-collection/canonical-build-evidence.json "$incoming"
          apply_chromatic_integration() {
            python -m hth.canonical_build_evidence merge --scope hth-chromatic-integration --base results-repo/normalization/chromatic-integration/canonical-build-evidence.json --incoming "$incoming" --output "$payload/canonical-build-evidence.json"
            rm -rf results-repo/normalization/chromatic-integration
            mkdir -p results-repo/normalization/chromatic-integration
            cp "$payload"/* results-repo/normalization/chromatic-integration/
            hth_results_stage results-repo normalization/chromatic-integration
          }
          hth_hardened_persist results-repo main "Publish chromatic integration ${{ steps.integration.outputs.result_identity }}" apply_chromatic_integration "Chromatic integration publication"
          echo "results_commit=$HTH_PERSIST_COMMIT" >> "$GITHUB_OUTPUT"
          printf '\n## Durable chromatic result\n\n- Chromatic result: [`%s`](%s)\n- Release activity: `%s`\n- Results commit: [`%s`](%s/%s/commit/%s)\n' "${{ steps.integration.outputs.result_identity }}" "${{ steps.release.outputs.release_url }}" "${{ steps.release.outputs.release_activity }}" "$HTH_PERSIST_COMMIT" "$GITHUB_SERVER_URL" "$RESULTS_REPOSITORY" "$HTH_PERSIST_COMMIT" >> "$GITHUB_STEP_SUMMARY"

      - name: Upload compact chromatic review
        if: steps.cbe_plan.outputs.decision == 'execute'
        uses: ./hth-pipeline/.github/actions/hardened-upload-artifact
        with:
          name: hth-chromatic-integration-review-${{ github.run_number }}
          path: |
            chromatic-integrated-collection/chromatic-normalization-manifest.json
            chromatic-integrated-collection/chromatic-normalization-manifest.csv
            chromatic-integrated-collection/summary.md
            chromatic-integrated-collection/release.json
          if-no-files-found: error
          retention-days: "${{ inputs.artifact_retention_days }}"
          compression-level: 6
